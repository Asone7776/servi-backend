model Media {
  id         Int           @id @default(autoincrement())
  url        String
  type       String
  size       Int?
  banners    BannerMedia[]
  created_at DateTime      @default(now())
  updated_at DateTime      @updatedAt

  @@map("media")
}
